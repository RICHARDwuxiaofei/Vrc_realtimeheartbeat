[project]
name = "vrc-realtime-heartbeat"
version = "1.2.0"
requires-python = ">=3.11"
dependencies = ["bleak>=3,<4", "qrcode[pil]>=8,<9"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-ra -p no:cacheprovider"
