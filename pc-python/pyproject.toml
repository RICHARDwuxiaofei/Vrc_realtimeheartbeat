[project]
name = "vrc-realtime-heartbeat"
version = "1.2.2-beta.1"
requires-python = ">=3.11"
dependencies = ["bleak>=3,<4", "qrcode[pil]>=8,<9", "zeroconf>=0.150,<1"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-ra -p no:cacheprovider"
