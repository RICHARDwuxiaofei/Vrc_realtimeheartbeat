# 更新日志

本项目从 `v1.1.0` 起采用此文件记录面向使用者和维护者的重要变化。日期使用仓库维护时区。

## [Unreleased]

### 复核修正

- 电脑端诊断曲线改为从内部 CSV 文件尾部反向读取，只解析所选 1–10 分钟窗口；长时间诊断不再每秒从文件头扫描全部历史。
- 手机端为用户触发的一键诊断保留独立待发槽，连续心率只覆盖“最新心率槽”，不会再把尚未发送的诊断请求挤掉。
- 手机 UDP socket 与目标电脑地址连接后再等待 ACK，避免其他主机用相同序号的数据包冒充电脑回执。
- “暂停发送到电脑”现在也会取消待发/在途诊断状态，不再只跳过连续心率。
- 普通模式额外移除 `sessionId`，确保会话标识和其他扩展诊断字段只在诊断模式传输。
- 正式手表版对同一警告事件做 60 秒限频，避免异常传感器状态反复写 Logcat/私有日志；诊断版继续保留完整事件。
- 手机明确禁用云备份和设备迁移，避免电脑地址等本机中转设置离开设备。
- 新增长时 CSV 窗口、诊断请求优先级和普通模式字段集合回归测试。

### 验证

- Python 52 项 pytest、源码自检、Tk UI 冒烟、单文件 EXE 构建及打包后自检通过。
- Android 三端/变体共执行 51 项单元测试，三套 Lint 均为 0 error，两个手表 APK 与手机 APK 构建成功。
- C# 回退桥、手表报告脚本自检和回退 EXE 构建成功。

## [1.1.0] - 待发布

### 新增

- Python Windows 端加入 Avatar 参数测试、GitHub 正式版检查和配对二维码。
- 加入默认关闭的跨端诊断模式：电脑通过 UDP ACK 控制手机，手机仅在状态变化时同步到手表。
- 诊断模式提供最近 1 分钟心率曲线，并可用滑轨选择 1–10 分钟；同步计算最低、最高和平均 BPM。
- 诊断数据追加写入内部 CSV；曲线可边写边读，用户导出必须手动触发，不会自动生成导出文件。
- 手机端加入二维码配对、手机到电脑一键诊断和完整诊断信息面板。
- 手表正式版加入 `1 秒实时 / 5 秒省电 / 10 秒超省电` 三档发送模式。

### 功耗优化

- 5/10 秒档只使用 Health Services 批量交付，不注册直接传感器、不持有手动 WakeLock。
- 正式版热路径改为内存状态更新，减少逐批 SharedPreferences、Logcat 和文件 flush。
- 缓存附近手机节点，对发现和失败重试增加退避；正式版手表 ACK 请求降为约每分钟一次。
- BPM 不变时使用双倍发送间隔保活，减少重复 Data Layer 通信。
- 普通模式不保留电脑端曲线历史、不写诊断 CSV，也不采集跨端扩展诊断字段。

### 兼容

- OSC 三位数参数继续按 `HR_Value → HR_Hundreds → HR_Tens → HR_Ones` 顺序发送。
- 保留 `HRValid`、本地生成的 `HRPulse` 以及旧版 `HeartRate*` 参数。
- 原 C# Windows 接收器保留为回退版本；新增桌面功能以 Python 接收器为准。
